## **2026-04-09**

**V3 Nonce 机制更新（所有 V3 文档）**

- 重写所有 V3 及测试网文档（中英文）中的 V3 Nonce 机制章节，改为结构化要点格式：
  - 明确服务端时间容差为 **10 秒**（原文描述为 5 秒）
  - 新增编号式请求处理流程说明：重复 nonce → 拒绝；过旧 nonce → 拒绝
  - 明确每用户最多维护 **100 个 nonce**，低于最小值则拒绝
  - 在测试网文档（合约 & 现货，中英文）中新增 Nonce 机制章节（原文档缺失）

**现货 API 更新**

- 在现货 V1 API（EN）中新增 `GET /api/v1/openOrder` — 查询当前挂单接口
- 在现货 V3 API（EN）中新增 `GET /api/v3/openOrder` — 查询当前挂单接口
