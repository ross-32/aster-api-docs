## **2026-04-24**

**Spot V3, Spot Testnet & Spot (Legacy) API Updates**

- Added three new transaction types to the `type` field of `GET /api/v3/transactionHistory` (Spot V3 & Testnet) and `GET /api/v1/transactionHistory` (Spot Legacy) — EN & ZH:
  - `STAKING_WITHDRAW`
  - `STAKING_CLAIM`
  - `STAKING_DELEGATE`
