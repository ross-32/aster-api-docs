## **2026-03-28**

**合约 V3 / 现货 V3 / 测试网 API 更新**

- 合约 V3 和合约测试网：接口 `GET /fapi/v3/account` 重命名为 `GET /fapi/v3/accountWithJoinMargin`
- 现货、现货 V3 和现货测试网：移除已废弃的 **创建 API Key**（`POST /api/v3/createApiKey`）和**获取 API Key Nonce**（`POST /api/v3/getNonce`）章节
- 合约 V3 和现货 V3：移除所有 V3 接口声明中的 `(HMAC SHA256)` 标注
- 现货测试网和合约测试网：更新 **MARKET_DATA** 鉴权类型为无需鉴权
- Python 代码示例更新（合约 V3、现货 V3、合约测试网、现货测试网基本信息）：
  - 新增 `import threading`，使用 `_nonce_lock` 实现线程安全的 `get_nonce()`
  - 合约测试网和现货测试网示例重构：改用 `encode_structured_data`，统一 `send_by_url` / `send_by_body` 函数签名
  - 合约 V3 示例新增 `batch_orders_delete` 调用演示及 DELETE HTTP 方法支持

**链上 RPC 更新**

- 更新隐私模式说明：隐私模式仅影响开启后的新订单，历史订单仍正常展示
- **查询当前挂单**：新增说明，仅返回 block 1 创世时间（`1772678119418`）之后的订单，最多返回 1000 条
- **查询用户成交记录**参数更新：`symbol`、`from`、`to` 改为可选；移除 `limit` 和 `page` 参数；支持最多查询 7 天范围
- 所有接口响应及字段说明中 `accountPrivy` 更名为 `accountPrivacy`
- 查询当前挂单和查询成交记录中 `symbol` 说明更新：传 `null` 或 `""` 查询所有交易对
